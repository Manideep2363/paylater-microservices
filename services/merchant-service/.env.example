SERVER_PORT=8083
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=paylater_merchants
JWT_SECRET=dev-secret-change-me

# Required for /internal/* routes (fail closed if empty).
# Callers must send header: X-Internal-Token: <value>
INTERNAL_API_TOKEN=change-me-internal-token
